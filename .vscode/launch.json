{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "WinMine (Debug)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\target\\debug\\winmine.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build",
            "stopAtEntry": false,
            "console": "integratedTerminal"
        }
    ]
}
