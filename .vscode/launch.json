{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "WinMine (Debug)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\Debug\\winmine.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build Debug",
            "stopAtEntry": false,
            "console": "integratedTerminal"
        }
    ]
}
